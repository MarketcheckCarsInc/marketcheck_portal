import styled from 'styled-components'
import { rem } from 'polished'

const H6 = styled.h6`
  font-size: ${rem(16)};
  font-weight: 900;
  line-height: ${19.2 / 16};
  text-transform: uppercase;
  margin-bottom: ${rem(7)};
`

export default H6
