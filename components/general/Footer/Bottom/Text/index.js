import styled from 'styled-components'
import { rem, em } from 'polished'

const Text = styled.p`
  font-size: ${rem(10)};
  line-height: ${12 / 10};
  margin-bottom: ${em(13, 10)};
`

export default Text
